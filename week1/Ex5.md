# Escape Sequences

Explain what 'escape characters' are. Make a table of the standard 'escape sequences' in C++; 'escape sequence' in the first column and (short) description in the second column. Lastly report what happens when an unrecognised 'escape sequence' is used and provide an example using a cout statement.

