**Describe six (major) aspects of C++ which are not available in C. Give for each of these aspects a short (1, at most 2 sentences) description what its purpose is.**

1. Function overloading: C++ allows multiple functions to have the same name. Because of the strict type checking, the compiler can distinguish between them based on their parameter types.

2. Default arguments: When defining a function using the assignment operator you can specify the default values used in the function if no value is provided.

3. C++ allows for object-oriented programming. 